export interface Locales {
  mainHeader: {
    title: string
    tag: string
    description: string
  }
  cartHeader: {
    title: string
    tag: string
    description: string
  }
  buttons: {
    addCart: string
    payBank: string
    payCash: string
  }
  item: {
    paymentTitle: string
  }
  currencySymbol: string
}
